import { ProductCatalog } from "@/widgets/product-catalog";

export const HomePage = () => {
    return (
        <section className="container">
            <ProductCatalog />
        </section>
    );
};